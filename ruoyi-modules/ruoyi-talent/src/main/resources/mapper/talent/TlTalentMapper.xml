<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.talent.mapper.TlTalentMapper">

    <!-- 人才主档查询列（与 tl_talent 表结构一致，不含逻辑删除过滤，由各语句显式声明） -->
    <sql id="talentColumns">
        talent_id,
        talent_no,
        name,
        gender,
        birth_date,
        age_only,
        age_source_date,
        phone_cipher,
        phone_hash,
        phone_tail4,
        education,
        expect_salary_min,
        expect_salary_max,
        position,
        contact_date,
        region_code,
        status,
        share_scope,
        source,
        remark,
        del_flag,
        create_dept,
        create_by,
        create_time,
        update_by,
        update_time
    </sql>

    <!-- 重复预检：按 phone_hash 精确命中（排除自身与已删除记录） -->
    <select id="selectByPhoneHash" resultType="org.dromara.talent.domain.TlTalent">
        select
        <include refid="talentColumns"/>
        from tl_talent
        where del_flag = '0'
        and phone_hash = #{phoneHash}
        <if test="excludeTalentId != null">
            and talent_id != #{excludeTalentId}
        </if>
    </select>

    <!-- 弱匹配：姓名 + 手机号后四位（排除自身与已删除记录） -->
    <select id="selectByNameAndTail4" resultType="org.dromara.talent.domain.TlTalent">
        select
        <include refid="talentColumns"/>
        from tl_talent
        where del_flag = '0'
        and name = #{name}
        and phone_tail4 = #{phoneTail4}
        <if test="excludeTalentId != null">
            and talent_id != #{excludeTalentId}
        </if>
    </select>

    <!-- 生成人才编号用：指定日期前缀下的最大编号 -->
    <select id="selectMaxTalentNo" resultType="java.lang.String">
        select max(talent_no)
        from tl_talent
        where del_flag = '0'
        <if test="datePrefix != null and datePrefix != ''">
            and talent_no like concat(#{datePrefix}, '%')
        </if>
    </select>

</mapper>
